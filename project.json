{
	"core": {
		"filesExclude": ["./docs/"]
	},
	"watch": {
		"rules": {
			"build": {
				"regex": "/([^/]+)/.+",
				"tasks": "npm run build --prefix $1"
			}
		}
	},
	"coaxial": {
		"worktree": {
			"setup": "ln -s ../../node_modules node_modules"
		},
		"tasks": {
			"steps": {
				"code_review": {
					"refinement": "Identify features that can be reused by other packages that belong in sdk."
				}
			}
		}
	},
	"ai": {
		"instructions": ["To run tests: npm test --prefix gbc/dirName"]
	}
}
