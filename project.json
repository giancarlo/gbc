{
	"core": {
		"filesExclude": ["./docs/"]
	},
	"watch": {
		"rules": {
			"build": {
				"regex": "/([^/]+)/.+",
				"tasks": "npm run build $1"
			}
		}
	},
	"ai": {
		"instructions": ["Run tests: npm test --prefix gbc/dirName"]
	}
}
